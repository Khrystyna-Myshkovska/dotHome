import { FilterRoomsPipe } from './filter-rooms.pipe';

describe('FilterRoomsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRoomsPipe();
    expect(pipe).toBeTruthy();
  });
});
